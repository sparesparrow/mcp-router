/**
 * Minimal shared package index
 * This is a temporary version that will compile during restructuring
 */

// Export types
export * from './types';

// Log that the minimal shared package has been loaded
console.log('Minimal shared package loaded'); 